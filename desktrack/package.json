{
    "name": "desktrack",
    "version": "1.0.0",
    "scripts": {
      "start:server": "json-server --watch backend/db.json --port 3000"
    },
    "devDependencies": {
      "json-server": "^0.17.0"
    }
  }